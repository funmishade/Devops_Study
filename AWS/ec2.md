## Concept
ELASTIC CLOUD COMPUTE ECC EC2


Compute instance - Virtual server with CPU, RAM, Disk
Elastic - can be scaled up or down

ECS saves you the stress of creating, upgrading, maintaining and costing of virtual machines yourself.

1. TYPES
General purpose
comute optimised
memory optimised
storage
accelerated

the type depends on the usecase

2. REGIONS and AZ

Regions are large geographic areas with multiple data centers, while Availability Zones (AZs) are physically separate locations within a single region

Region - us-east-1
avaikability zone - us-east-1a, put application in myltiple AZ for HA

3. TYPE OF OPERATUNG SYSTEM - Ubuntu, redhart etc

4. INSTANCE TYPE

5. KEY PAIR - used to authenticate, keyvalue pair, combination of public private key, ec2 has public key while you have private key



1. request ecw instance close to client as much as possible
## Key Commands / Syntax

ssh -i aws_login.pem ubuntu@54.242.97.240
## Example
## Takeaway


## New Learnings



## Questions
5. KEY PAIR - used to authenticate, keyvalue pair, combination of public private key, ec2 has public key while you have private key